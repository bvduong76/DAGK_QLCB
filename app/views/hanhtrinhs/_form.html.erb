<%= form_for(hanhtrinh) do |f| %>
  <% if hanhtrinh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hanhtrinh.errors.count, "error") %> prohibited this hanhtrinh from being saved:</h2>

      <ul>
      <% hanhtrinh.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :sanbaydi %>
    <%= collection_select( :sanbays, :masanbay , Sanbay.all , :id, :tensanbay, {:prompt => true, :multiple=>true } ) %>
  </div>

  <div class="field">
    <%= f.label :sanbayden %>
    <%= collection_select( :sanbays, :masanbay , Sanbay.all , :id, :tensanbay, {:prompt => true, :multiple=>true } ) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
